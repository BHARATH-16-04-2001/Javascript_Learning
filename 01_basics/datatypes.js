"use strict"  // treated the js code into newer version

// alert("hello") // this is node not browser it will throw the error

console.log("hi"); console.log("Bharath");  // not good practice

let age  =  18        // number = 2 pow 53
                      // bigint 
let name = "Bharath"  // string = " "
let condition = true  // boolean = true / false
let state  =  null    // null  = standanole value
let phone;            // undefined 
                      // Symbols = unique

console.log(typeof undefined);   // undefined
console.log(typeof(null));       // object


